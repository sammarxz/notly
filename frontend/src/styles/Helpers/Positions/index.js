import { css } from 'styled-components';

const Positions = css`
  .p--relative {
    position: relative;
  }

  .p--absolute {
    position: absolute;
  }

  .p--fixed {
    position: fixed;
  }
`;

export { Positions };